# IT Analysis

Provide more information


## Setup Guide

1. Run `virtualenv .venv`
2. Activate your virtual env `source .venv/bin/activate`
3. Install your dependencies `pip install -r requirements.txt`
4. Run your code `python it_analysis.py`
